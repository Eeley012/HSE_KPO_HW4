# Microservices Shop

Асинхронное взаимодействие микросервисов через Apache Kafka.

## 🏗 Архитектура

Система состоит из двух микросервисов:

1.  **Orders Service**
    * Создает заказы со статусом `New`.
    * Отправляет событие `OrderCreated` в Kafka.
    * Слушает результат оплаты и меняет статус на `Paid` или `Failed`.
2.  **Payments Service**
    * Управляет счетами пользователей.
    * Слушает Kafka.
    * При поступлении заказа проверяет баланс, списывает средства и отправляет результат обратно в Kafka.

**Инфраструктура:**
* **Apache Kafka**: Брокер сообщений.
* **Zookeeper**: Координатор для Kafka.
* **Docker Compose**: Оркестрация контейнеров.

## 🚀 Как запустить

Требуется установленный Docker Desktop.

1.  Клонируйте репозиторий.
2.  Перейдите в папку проекта.
3.  Запустите сборку и старт контейнеров:

```bash
docker-compose up --build -d
```

PS
> **⚠️ Холодный старт:** Kafka может инициализироваться пару десятков секунд после запуска контейнера
